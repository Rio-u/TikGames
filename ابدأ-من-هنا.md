# TikGames — ابدأ من هنا

## التشغيل: خطوة واحدة

**دبل كليك على `setup.bat`** واستنى.

هيطلع لك طلب موافقة من ويندوز — اضغط **نعم** (محتاجينها عشان نثبّت MongoDB).

الملف ده بيعمل كل حاجة لوحده:

1. بيتأكد من `winget`
2. بينزّل **Node.js** لو مش موجود (أو لو الإصدار أقدم من 20)
3. بينزّل **pnpm**
4. بينزّل **MongoDB**
5. بيثبّت حزم المشروع
6. بيجهّز ملفات `.env`
7. بيشغّل MongoDB على بورت 27018
8. بيجهّز قاعدة البيانات
9. بيعمل الحسابات ويحط خلفيات الألعاب
10. بيشغّل المشروع كله ويفتح المتصفح

أول مرة بتاخد من **5 لـ 15 دقيقة** (على حسب النت، معظمها تنزيل MongoDB). المرات اللي بعدها **أقل من دقيقة** — بيلاقي كل حاجة متظبطة وبيعدّيها.

> تشغيله تاني في أي وقت مفيهوش ضرر. مش بيمسح داتا ولا بيرجّع باسورد غيّرته.

---

## الدخول

بعد ما يخلّص هيفتح لك <http://localhost:5173>.

**سجّل دخول بالإيميل — مش باليوزرنيم:**

| | الإيميل | الباسورد |
|---|---|---|
| حسابك | `nfnf@tikgames.local` | `123456789` |
| الأدمن | `d7@tikgames.local` | `123456789` |

الاتنين **اشتراكهم دائم** — مفيش حد للألعاب ولا تجربة بتخلص.

حساب الأدمن بيفتح لوحة التحكم على <http://localhost:5173/d7admind7> — منها بترفع خلفيات وموسيقى، وبتقفل/بتفتح أي لعبة، وبتشوف سجل النشاط.

---

## تلعب إزاي من غير تيك توك حقيقي

الاتصال الحقيقي بتيك توك محتاج مفتاح توقيع مدفوع/مجاني من [eulerstream.com](https://www.eulerstream.com)، **بس إنت مش محتاجه عشان تجرّب**:

1. من الداشبورد اضغط **ابدأ البث** واكتب **أي** اسم قناة (`test` مثلاً).
   - اللايف هيروح لحالة `ERROR` بعد ثواني — **ده طبيعي ومتوقع**، وبيفضل شغال عادي.
2. افتح صفحة أي لعبة واضغط **ابدأ اللعبة**.
3. افتح تاب جديد على <http://localhost:5173/live/simulate> وابعت كومنتات وهمية.

الكومنتات الوهمية بتعدّي على **نفس مسار الكود** بالظبط اللي بتعدّي عليه كومنتات تيك توك الحقيقية — فاللي بتشوفه هو اللي هيحصل في اللايف الحقيقي.

### الأوفرلاي في OBS

<http://localhost:5174> هي صفحة الأوفرلاي (خلفية شفافة). من صفحة اللعبة في الداشبورد بتلاقي رابط الأوفرلاي بالتوكن بتاع الغرفة — حطه في OBS كـ **Browser Source**.

---

## الروابط

| | |
|---|---|
| الداشبورد | <http://localhost:5173> |
| الأوفرلاي (للـ OBS) | <http://localhost:5174> |
| الـ API | <http://localhost:4000/health> |
| لوحة الأدمن | <http://localhost:5173/d7admind7> |
| كومنتات وهمية | <http://localhost:5173/live/simulate> |

---

## أوامر تانية

| عايز إيه | اعمل إيه |
|---|---|
| توقّف كل حاجة | `stop.bat` |
| تشغّل تاني بعد ما وقّفت | `setup.bat` (سريع في المرة دي) |
| تشوف إيه اللي حصل لو حاجة وقعت | افتح فولدر `.logs\` |
| تتفرّج على الداتابيز | `pnpm db:studio` |
| ترجّع الحسابات لو غيّرت الباسورد | `cd apps\api` وبعدين `set SEED_RESET_PASSWORDS=1` وبعدين `node seed.mjs` |

---

## لو حاجة وقعت

**الشباك قفل من غير ما يقول حاجة** — شغّل `setup.bat` من داخل الـ Command Prompt عشان تشوف الرسالة، أو افتح `.logs\`.

**"winget مش موجود"** — نزّل [App Installer](https://apps.microsoft.com/detail/9nblggh4nns1) من متجر مايكروسوفت، أو نزّل [Node.js](https://nodejs.org) و [MongoDB](https://www.mongodb.com/try/download/community) بإيدك وبعدين شغّل `setup.bat` تاني.

**"Node.js اتنزّل بس لسه مش ظاهر"** — اقفل الشباك وافتح `setup.bat` تاني. ويندوز محتاج يحدّث الـ PATH.

**اللعبة بتقول "مفيش خلفيات مرفوعة"** — يبقى فولدر `seed-assets\trivia-backgrounds\` (أو `speed-word-backgrounds\`) فاضي. حط فيه صور وشغّل `node apps\api\seed.mjs`، أو ارفعها من لوحة الأدمن على طول.

**البورت مشغول** — `stop.bat` وبعدين `setup.bat`.
