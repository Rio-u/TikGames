# TikGames — ابدأ من هنا

<div align="center">
  <img src="https://raw.githubusercontent.com/Rio-u/TikGames/main/logo.png" alt="TikGames logo" width="180" />
</div>

## 🚀 التشغيل: خطوة واحدة

**دبل كليك على `setup.bat`** واستنى.

هيطلع لك طلب موافقة من ويندوز — اضغط **نعم** عشان يقدر يثبّت المتطلبات (Node و pnpm).

الملف ده بيشتغل لوحده ويعمل كل حاجة:

1. بيتأكد من `winget`
2. بينزّل **Node.js** لو مش موجود أو لو الإصدار أقدم من 20
3. بينزّل **pnpm**
4. بيثبّت الحزم المطلوبة
5. بيجهّز ملفات `.env`
6. بيتأكد إن `DATABASE_URL` بتاع Supabase متظبط
7. بيجهّز قاعدة البيانات (الجداول على Supabase)
8. بيعمل الحسابات الأساسية ويضيف خلفيات الألعاب
9. بيشغّل المشروع كله ويفتح المتصفح

> **قبل أول تشغيل — قاعدة البيانات (مرة واحدة):** قاعدة البيانات بقت **Supabase** (على السحابة،
> مجاني) — مفيش MongoDB بتتثبت على جهازك. اعمل مشروع على https://supabase.com، وبعدين من زر
> **Connect** انسخ رابط الـ **Session pooler** وحطّه في `DATABASE_URL` جوه الملفين دول:
> `packages/database/.env` و `apps/api/.env`. لو نسيت، `setup.bat` هيوقف عند خطوة 6 ويقول لك
> بالظبط تعمل إيه. (التفاصيل الكاملة في `DEPLOY.md` القسم ١.)

أول مرة بتاخد من **5 لـ 15 دقيقة** حسب سرعة الإنترنت، واللي بعد كده **أقل من دقيقة** لأن كل حاجة متجهّزة فعلاً.

> ✅ تشغيله تاني في أي وقت ما بيضرش. لا بيحذف بيانات ولا بيفكّك الباسوردات اللي غيّرتها.

---

## 🔐 الدخول

بعد ما يخلص، هيفتح لك <http://localhost:5173> مباشرة.

**سجّل دخول بالإيميل — مش باليوزرنيم:**

| الحساب | الإيميل | الباسورد |
|---|---|---|
| حسابك | `nfnf@tikgames.local` | `123456789` |
| الأدمن | `d7@tikgames.local` | `123456789` |

الاتنين **اشتراكهم دائم**، ومفيش حد للألعاب ولا تجربة بتتخلص.

حساب الأدمن بيفتح لوحة التحكم على <http://localhost:5173/d7admind7> — منها بترفع الخلفيات والموسيقى، وتغلق/تفتح أي لعبة، وتشوف سجل النشاط.

---

## 🎮 تلعب إزاي من غير تيك توك حقيقي

الاتصال الحقيقي بتيك توك محتاج مفتاح توقيع مدفوع/مجاني من [eulerstream.com](https://www.eulerstream.com)، **لكن مش محتاجه عشان تجرّب**.

### شغّل اختبار سريع

1. من الداشبورد اضغط **ابدأ البث** واكتب **أي** اسم قناة مثل `test`.
   - اللايف هيطلع بحالة `ERROR` بعد ثواني — **ده طبيعي ومتوقع**، وبيفضل شغال بشكل عادي.
2. افتح أي لعبة واضغط **ابدأ اللعبة**.
3. افتح تب جديد على <http://localhost:5173/live/simulate> وابعت كومنتات وهمية.

الكومنتات الوهمية بتعدّي على **نفس مسار الكود** بالظبط اللي بتعدّي عليه كومنتات تيك توك الحقيقية؛ فالمتسجل في الواجهة هو نفس اللي هيحصل في البث الحقيقي.

### 📺 الأوفرلاي في OBS

<http://localhost:5174> هي صفحة الأوفرلاي (خلفية شفافة). من داخل صفحة اللعبة في الداشبورد، بتلاقي رابط الأوفرلاي بالتوكن الخاص بالغرفة — خده وضعه في OBS كـ **Browser Source**.

---

## 🔗 الروابط السريعة

| الاسم | الرابط |
|---|---|
| الداشبورد | <http://localhost:5173> |
| الأوفرلاي (للـ OBS) | <http://localhost:5174> |
| الـ API | <http://localhost:4000/health> |
| لوحة الأدمن | <http://localhost:5173/d7admind7> |
| كومنتات وهمية | <http://localhost:5173/live/simulate> |

---

## 🛠️ أوامر مفيدة

| عايز إيه | اعمل إيه |
|---|---|
| توقّف كل حاجة | `stop.bat` |
| تشغّل تاني بعد ما وقّفت | `setup.bat` (أسرع في المرة دي) |
| تشوف إيه اللي حصل لو حاجة وقعت | افتح مجلد `.logs\` |
| تتفرّج على الداتابيز | `pnpm db:studio` |
| ترجّع الحسابات لو غيّرت الباسورد | `cd apps\api` ثم `set SEED_RESET_PASSWORDS=1` ثم `node seed.mjs` |

---

## ⚠️ لو حصلت مشكلة

**الشباك قفل من غير ما يقول حاجة** — شغّل `setup.bat` من داخل **Command Prompt** عشان تشوف الرسالة، أو افتح مجلد `.logs\`.

**"winget مش موجود"** — نزّل [App Installer](https://apps.microsoft.com/detail/9nblggh4nns1) من متجر مايكروسوفت، أو نزّل [Node.js](https://nodejs.org) يدويًا، وبعدها شغّل `setup.bat` تاني.

**"Node.js اتنزّل بس لسه مش ظاهر"** — اقفل الشباك وافتح `setup.bat` تاني. ويندوز غالبًا محتاج يحدّث الـ `PATH`.

**اللعبة بتقول "مفيش خلفيات مرفوعة"** — فاكهة المشكلة إن مجلد `seed-assets\trivia-backgrounds\` (أو `speed-word-backgrounds\`) فاضي. حط فيه صور، ثم شغّل `node apps\api\seed.mjs` أو ارفعها من لوحة الأدمن.

**البورت مشغول** — استخدم `stop.bat` ثم `setup.bat` من جديد.

---

<div align="center">
  <strong>مستعد؟</strong>
  <br>
  شغّل <code>setup.bat</code> وابدأ اللعب.
</div>
