{
  "name": "@tikgames/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "TikGames desktop shell (Electron). A thin, hardened client: it fingerprints the machine, activates against the licensing API with the product password, waits for admin approval, then loads the dashboard. No secrets ship in the build.",
  "main": "dist/main.js",
  "scripts": {
    "build": "node scripts/stamp-config.mjs && tsc -p tsconfig.json",
    "dev": "pnpm build && electron .",
    "dist": "node scripts/stamp-config.mjs && tsc -p tsconfig.json && electron-builder --win --x64",
    "stamp": "node scripts/stamp-config.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^32.2.7",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.0"
  }
}
