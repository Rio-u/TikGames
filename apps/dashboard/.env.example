VITE_API_URL=http://localhost:4000

# Cloudflare Turnstile — dash.cloudflare.com, "Add a site". Public site key, safe to expose
# client-side. Leave empty to disable the CAPTCHA widget entirely (registration still works).
VITE_TURNSTILE_SITE_KEY=
